{
  "name": "Friendship",
  "type": "object",
  "properties": {
    "friend_email": {
      "type": "string",
      "description": "Email of the friend"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "accepted"
      ],
      "default": "pending",
      "description": "Friendship status"
    }
  },
  "required": [
    "friend_email"
  ]
}